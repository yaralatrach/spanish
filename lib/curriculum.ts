/** New words introduced per day, after the journal gate and any due review. */
export const NEW_WORDS_PER_DAY = 10;

/** Words shown per sweep batch. Large enough to move fast, small enough to
 *  keep one screen scannable. */
export const SWEEP_BATCH = 100;
